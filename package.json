{
  "name": "@htmlplus/dev",
  "scripts": {
    "bootstrap": "lerna run build --scope=@htmlplus/element && lerna run build --scope=@htmlplus/core && lerna run build --scope=@htmlplus/react && lerna run build --scope=@htmlplus/examples",
    "clean": "lerna run clean",
    "clean:hard": "npm run clean && lerna clean",
    "postinstall": "lerna bootstrap --force-local"
  },
  "dependencies": {
    "@netlify/plugin-nextjs": "^4.2.6",
    "lerna": "^3.20.2"
  }
}
