import { Context } from '@app/types';

export const typescript = () => {

    const name = 'typescript';

    const next = (context: Context) => {

    }

    return {
        name,
        next,
    }
}