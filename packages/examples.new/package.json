{
  "name": "@htmlplus/examples.new",
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.js",
    "clean": "rimraf dist",
    "dev:build:watch": "nodemon scripts/build.js --ignore src/*"
  },
  "devDependencies": {
    "@htmlplus/element": "latest",
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2"
  }
}
