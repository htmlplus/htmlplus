import React from 'react';
import ReactDOM from 'react-dom';
{{#if hasStyle}}
import './index.css';
{{/if}}

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);