export default {
  key: 'cropper',
  examples: [
    require('./default').default,
    require('./aspect-ratio').default,
    require('./backdrop').default,
    require('./background').default,
    require('./guides').default,
    require('./indicator').default,
    require('./mode').default,
    require('./viewport').default,
  ]
}