export default {
  key: 'spinner',
  examples: [
    require('./default').default,
    require('./size').default,
    require('./custom-size').default,
    require('./color').default,
    require('./type').default,
    require('./customize').default,
  ]
}