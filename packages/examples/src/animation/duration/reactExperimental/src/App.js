import '@htmlplus/core/animation.js';

const AnimationDuration = () => {
  return (
    <plus-animation
      name="fade-in"
      duration="2500"
      iterations="Infinity"
      play
    ></plus-animation>
  );
};

export default AnimationDuration;
