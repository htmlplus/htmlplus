import { Avatar, Icon } from '@htmlplus/react';

const AvatarIcon = () => {
  return (
    <div className="center">
      <Avatar>
        <Icon>user</Icon>
      </Avatar>
    </div>
  );
};

export default AvatarIcon;
